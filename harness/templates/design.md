# <模块名> 设计文档

## 背景与目标

说明该模块解决什么问题，以及成功的衡量标准。

## 功能行为

描述模块对外暴露的行为：接收什么输入、产生什么输出、在什么条件下触发什么效果。
用自然语言或表格描述，**不得出现函数签名、字段名、SQL、算法步骤等代码细节**。

## 边界与约束

列出该模块明确不处理的场景，以及性能、安全、一致性等约束。

## 依赖关系

该模块依赖哪些外部系统或内部模块（只列名称和交互方式，不描述实现）。

## 代码实现索引

> 以下为代码位置索引，便于在 spec 与实现之间导航。索引内容随代码变化更新。

| 职责 | 文件路径 |
|------|---------|
| （初始化时留空，首次实现后填写） | [path/to/file.ts](path/to/file.ts) |

---

> spec 是功能行为的唯一信源，代码是实现的唯一信源。
> spec 中不得描述代码细节（函数签名、内部数据结构、算法步骤等）。
> 功能行为变更 → 先改 spec；实现细节变更 → 只改代码。
>
> 当本文件超过 1000 行或出现明显独立的子职责时，将模块拆分为子目录：
> `docs/design/<module>/README.md`（总览）+ 各子模块独立 spec 文件。
